using UnityEngine;
using System.Collections;

public class SeletCampAdmin : MonoBehaviour {

    /*
	// Use this for initialization
    static private bool addcompornent= false;
	void Start () {
        if(!addcompornent)
        {
            GameAdmin.instance.gameObject.AddComponent<SeletCampAdmin>();
            addcompornent = true;
        }
	}
	
	// Update is called once per frame
	void Update () {
	
	}
    void OnDestroy()
    {
        Destroy(GameAdmin.instance.gameObject.GetComponent < SeletCampAdmin>());
    }
     * */
}
